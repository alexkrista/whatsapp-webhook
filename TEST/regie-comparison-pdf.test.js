"use strict";
const assert=require("assert"),fs=require("fs"),path=require("path"),root=path.resolve(__dirname,"..");
const ui=fs.readFileSync(path.join(root,"public","ui","baustellen-knowledge-hub.js"),"utf8"),server=fs.readFileSync(path.join(root,"server.js"),"utf8"),pdf=fs.readFileSync(path.join(root,"regie-comparison-pdf.js"),"utf8");
assert.match(ui,/Stundenabgleich als PDF/);assert.match(ui,/BaustellenLiveHours\?\.refresh/);assert.match(ui,/personDayHours/);assert.match(ui,/Pausen sind in den Netto-Stempelstunden bereits abgezogen/);assert.match(ui,/kein Bericht/);assert.doesNotMatch(ui,/<h3>Stundenabgleich pro Mitarbeiter und Tag<\/h3>/);assert.match(server,/regie-comparison\.pdf/);assert.match(server,/x\.regie===null/);for(const text of ["Gestempelte Stunden","Regiestunden laut PDF","Gesamtdifferenz","Stundenwert","Material","kein Bericht"])assert.ok(pdf.includes(text));console.log("regie comparison pdf test: ok");
