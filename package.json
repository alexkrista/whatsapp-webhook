{
  "name": "kristine-hotfix-2026-08-18",
  "version": "1.0.0",
  "private": true,
  "description": "Hotfix for immediate WhatsApp fertig stop and KRISPLAN absence reconciliation",
  "type": "commonjs",
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "node --check lib/finish-now.js && node --check lib/krisplan-absence-reconcile.js && node --check integration/server-wiring.example.js"
  },
  "engines": {
    "node": ">=18"
  }
}
